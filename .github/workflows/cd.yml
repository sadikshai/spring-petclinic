---
    name: continous deployement
    on:
        push:
            branches:
                - release
    jobs:
      deployment_cluster:
         runs-on: ubuntu-latest
         permissions:
          id-token: write 
          contents: read 

         steps: 
          - name: Checkout code
            uses: actions/checkout@v4
    
    
          - name: Log in to Docker Hub
            uses: docker/login-action@v2
            with:
              username: sadik8143
              password: Sadik@8143
          - name: pulling the image
            run: |
              docker pull sadik8143/springpetclinic:1.0

          - name: installing trivy
            run: |
                sudo apt-get install wget apt-transport-https gnupg lsb-release
                wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | gpg --dearmor | sudo tee /usr/share/keyrings/trivy.gpg > /dev/null
                echo "deb [signed-by=/usr/share/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb $(lsb_release -sc) main" | sudo tee -a /etc/apt/sources.list.d/trivy.list
                sudo apt-get update
                sudo apt-get install trivy -y

      
          - name: Scan the image and save results
            run: |
                  trivy image --format json --output trivy-results.json sadik8143/springpetclinic:1.0

      
          - name: Upload Trivy scan results
            uses: actions/upload-artifact@v3
            with:
                name: trivy-results
                path: trivy-results.json 

           
          - name: Azure login
            uses: azure/login@v2
            with:
              clientId: "03167f04-6443-4cd7-8e95-3da187c6858b"
              displayName: "myServicePrincipal"
              password: "gAj8Q~lJaQMrKR9SZcCTLUc-jmlSalDatAN54akG"
              tenant: "99ef06df-664d-4769-9eaa-9f4ba6706a0d"
            
    


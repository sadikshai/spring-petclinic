apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spc-chart.fullname" . }}-test-connection"
  labels:
    {{- include "spc-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spc-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
